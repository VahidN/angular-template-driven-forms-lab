import { MomentJalaaliPipe } from './moment-jalaali.pipe';

describe('MomentJalaaliPipe', () => {
  it('create an instance', () => {
    const pipe = new MomentJalaaliPipe();
    expect(pipe).toBeTruthy();
  });
});
